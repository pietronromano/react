import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // Server-side render is true by default, to enable SPA mode set this to `false`
  ssr: false,
} satisfies Config;
